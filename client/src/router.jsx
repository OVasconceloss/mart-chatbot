import { BrowserRouter, Routes} from 'react-router-dom';

export const Router = () => {
    return (
        <BrowserRouter>
            <Routes>
            </Routes>
        </BrowserRouter>
    )
}